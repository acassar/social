# apps/web
VITE_API_URL=http://localhost:3000

# Web Push (M6-T2) — clé publique VAPID, doit être identique à
# VAPID_PUBLIC_KEY côté apps/api/.env. Sans elle, le bouton d'opt-in par
# salon reste masqué (pas de notifications possibles depuis ce build).
VITE_VAPID_PUBLIC_KEY=
